name: Auto commit

on:
  schedule:
    - cron: "0 0 * * 0,1,3,4,6"
  workflow_dispatch:
    inputs:
      total_commit_number:
        description: "The number of commits to generate"
        default: "5"

jobs:
  auto_commit:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3
        with:
          repository: Parixshit/Green
          token: ${{ secrets.PAT }}
          ref: main

      - name: Configure Git
        run: |
          git config --local user.email "$GITHUB_ACTOR_ID+$GITHUB_ACTOR@users.noreply.github.com"
          git config --local user.name "$GITHUB_ACTOR"

      - name: Download Emojis
        env:
          EMOJI_API_KEY: ${{ secrets.EMOJI_API_KEY }}
        run: |
          curl -s "https://emoji-api.com/categories/activities?access_key=$EMOJI_API_KEY" | jq -r '.[].character' > emojis.txt

      - name: Generate Commits
        run: |
          total_commit_number=${{ inputs.total_commit_number }}
          if [[ -z "$total_commit_number" ]]; then
            total_commit_number=$(( RANDOM % 30 + 1 ))
          fi
          for random_commit_number in $(seq $total_commit_number); do
            if [[ $total_commit_number -gt 30 ]]; then
              commit_date=$(date -d "-$(( RANDOM % 365 + 1 )) days ago" +"%a, %d %b %Y %T")
            else
              commit_date=$(date +"%a, %d %b %Y %T")
            fi
            emoji=$(shuf -n 1 emojis.txt)
            sed -i "1s|.*|**[$GITHUB_ACTOR Committed on: ]($GITHUB_SERVER_URL/$GITHUB_REPOSITORY/commit/$GITHUB_SHA) $commit_date** <!-- $(git rev-parse HEAD) -->|" README.md
            git add README.md
            git commit --date="$commit_date" -s -m "$emoji Commit $random_commit_number on $commit_date"
          done

      - name: Push Changes
        run: git push origin main
